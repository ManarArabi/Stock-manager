{
  "name": "stock-manager-server",
  "version": "0.0.0",
  "description": "StockManagerServer",
  "main": "app.js",
  "author": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "export NODE_ENV=developement && node src/app.js",
    "production": "export NODE_ENV=production && node src/app.js"
  },
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^3.0.7",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongodb": "^3.4.1",
    "mongoose": "^5.8.3",
    "mongoose-unique-validator": "^2.0.3",
    "validator": "^12.1.0"
  }
}
